#Задаем список не возрастающих елементов елементов. Числа от 100 до 1 через 3, парные повторяются.
L = [] #L - list, не лучшее название переменной, но мне так было проще читать код
for i in range (100,1,-3):
    L.append(i)
    if i % 2 ==0:
        L.append(i)
print("Список чисел по не возрастанию")
print(L)
#Число, которое будет добавлено в список по рейтингу
numberToAdd = int(input("Введите число для добавления в рейтинг")

#Не со всей силы лаконично, но я все протестил - работает)
#Есть чуйка, что существуют более елегантные решения
#Реализация через функцию, чтоб сохранить себе на будущее

def addNumber(num, cicleRunCounter = True):
    #Если cicleRunCounter = True - будет отображаться количество пробежек цикла
    
    i = len(L)//2 #Делим список на пополам, и смотрим посторонам
    
    #Если добавляемое число меньше самого меньшего, или больше самого большего - добавляем сразу в начало, или конец соответсвенно
    if L[0]<=num:
        L.insert(0,num)
        if cicleRunCounter == True:
            return (L,"Cicle runs = 1")
        else:
            return (L)
    elif L[len(L)-1]>=num:
        L.append(num)
        if cicleRunCounter == True:
            return (L,"Cicle runs = 1")
        else:
            return (L)
        #Использовал перебор через while True
    j = 2 #Итератор
    while True:
        if num>=L[i] and num<=L[i-1]: #Сравниваем соседние числа, если попали в нужное место - вставляем
            L.insert(i,num)
            if cicleRunCounter == True: #реализация выведения количества пробежек
                return (L,"Cicle runs = {}".format(j-1))
            else:
                return (L)
        elif num<L[i]: #Смотрим вправо
             if len(L)//(2**j)>0: #Без этой строчки цикл может стать бесконе
                 i = i+len(L)//(2**j) #Каждый раз отсекаем половину списка, где нету нужного елемента
             else:
                 i = i+1 
        elif num>L[i]:#Смотрим влево
            if len(L)//(2**j)>0:
                i = i- len(L)//(2**j)
            else: 
                i=i-1
        j=j+1

##Начало программы##
print(addNumber(numberToAdd,True))
