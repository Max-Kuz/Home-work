#1 З/п = з/п в час * часы работы + премия. Запускается через терминал
print ('Суммарный заработок составит {} рублей'.format(int(input('Введите з/п в час: '))*int(input('Введите кол-во отработаных часов: '))+int(input('Введите размер премии: '))))

#2 Генерирует список случайной длины, состоящи из случайных целых чисел, и выводит
# Выводим числа из предыдущего списка, который бОльшие предыдущего

import random
length = random.randint(10,20)
lis = []
for i in range(length):
    lis.append (random.randrange(1,100,3))
print (lis)

lisWork = []
for i in range(length-1):
    if lis[i+1]<lis[i]:
        lisWork.append(lis[i])
print (lisWork)

#3 Для чисел в пределах от 20 до 240 найти числа, кратные 20 или 21. Необходимо решить задание в одну строку.

print([el for el in range(20,241) if (el % 20 == 0)or(el % 21 ==0)])

#4
#5
#6
