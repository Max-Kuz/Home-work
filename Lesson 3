#По возможности реализовал программы с аргументами, задаваемыми в коде, а не через input, чтоб проще было проверять

#1.Деление двух цифр (первую на вторую). Выдает ошибку при попытке деления на ноль
def division (a,b):
    try:
        return (a/b)
    except:
        print('ой!')
        
print(division (1,10))
print(division (1,0))        
print ('{:.2f}'.format(division (1,3))) 



#2.Выводит данные о пользователе предложением

def userInfo (name, surname, DOB, city, email, phone):
    print ('Пользователь {} {} родился {} в городе {}. Его емейл: {}, его номер телефона: {}'.format(name, surname, DOB, city, email, phone))
    
userInfo('Максим','Кузнецов','25.06.1997','Львов','maksym.v.kuznetsov@gmail.com', '+380637290711')




#3.Функция запрашивает 3 числа, возвращает сумму двух бОльших

def returnTwoBigger (a,b,c):
    return(a+b+c-min(a,b,c))
    
print(returnTwoBigger(1,2,3))




#4.Возведение целого числа в целую отрицательную степень двумя способами

def toThePowerOf_type1 (x,y):
    try:
        y>=0
    except:
        return ('y< - y should be int and < 0')
    return (x**y)
    
def toThePowerOf_type2 (x,y):
    try:
        y>=0
    except:
        return ('y< - y should be int and < 0')
    result = 1
    for i in range(-y):
        result = result/x
    return(result)

print(toThePowerOf_type1 (2,-3))
print(toThePowerOf_type2 (2,-3))




#5.Вводим числа через пробел. Программа выводит их сумму и снова запрашивает числа через пробел - они будут добавлены к уже имеющейся сумме. 
#Чтоб остановится - нужно ввести "s" (так же через пробел)
#Если ввести что-то не то (например пробел в конце предложения) - результат не забывается, "стоп-слово" будет напомнено
#Реализация через рекурсию

def sumation(sum=0):
    try:
        numbers = input('Put numbers over spaces. To stop the programm - type "s"')
        numbers = numbers.split(' ')
        #print(numbers)
        for i in range(len(numbers)):
            if numbers[i] == 's':
                return(sum)
            sum = sum + int(numbers[i])
        return(sumation(sum))
    except:
        print('whoops! A wrong type was put. I need integers. \nYour result is safed up to this point \nTo quit - press "s"')
        return(sumation(sum))

print(sumation())




#6.Capital делает первую букву слова (введеного str) заглавной. Я так полагаю, что надо было сделать это без использования встроенной функции 
#CapitalSentance делает предложение (слова, введенные через пробел) заглавными. CapitalSentance использует Capital

def Capital(aWord='qwrety'):
    if len(aWord) < 2:
        return(aWord.upper())
    else:
        cap = aWord[0].upper()
        aWord = cap+aWord[1:]
        return(aWord)

def CapitalSentance(aSentance = 'Lorem ipsum dolor sit amet, \nconsectetur adipiscing elit, \nsed do eiusmod tempor incididunt \nut labore et dolore magna aliqua'):
    aSentance = aSentance.split(' ')
    theSentance = ''
    for i in range(len(aSentance)):
            theSentance = theSentance +' '+Capital(aSentance[i])
    return(theSentance)
    
print(CapitalSentance())
